# HTML-basics-mark-text
Mark text means the text gets highlighted in a yellow highlight colour (by default the colour is yellow). In order to use it, use the key &lt;mark> put the text inside and close.
